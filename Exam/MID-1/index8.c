/*
Numerical value or sequence of ASCII characters. The 128 ASCII standard characters range from 0 to 127. Extended ASCII expands by 128 more values to a total of 255. Usually, numbers are shown in hexadecimal or decimal.

*/
#include<stdio.h>
int main()
{
    char ch='a';
    int b=ch;
    printf("%d",b);
    
}